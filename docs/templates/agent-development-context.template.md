---
module: "{{module_name}}"
title: "{{agent_chinese_name}}"
language: "{{Python / TypeScript / Java / Go / ...}}"
agent_type: "{{Tool-Using Agent / RAG Agent / Workflow Agent / Multi-Agent / ...}}"
last_updated: "{{YYYY-MM-DD}}"
---

# {{agent_chinese_name}} Agent 开发上下文

> **文档定位**  
> 本文档是面向人类开发者和 AI Coding 工具的 Agent 开发上下文文档。  
> 它用于约束该 Agent 的角色定位、能力边界、Harness 架构、可调用工具、上下文来源、核心业务流、数据模型和测试要求。  
> 本文档不是终端用户说明，也不是运行时 Prompt。

> **AI 阅读契约**  
> AI Coding 工具在生成、修改或重构代码时，必须遵守本文档定义的角色边界、工具边界、数据边界和业务流程。  
> 不得擅自扩大 Agent 权限，不得绕过声明的 Tool / Service / Repository 直接操作数据库、文件系统、外部接口或其他底层资源。  
> 不得新增未声明的核心流程、外部依赖、高风险操作或数据写入行为。  
> 涉及具体 Agent 的新增、修改或重构时，AI Coding 工具必须先读取对应 Agent 开发上下文文档，并以该文档作为实现边界和验收依据。  
> 如果实现前发现该文档与目标需求不一致，或需要调整 Agent 的角色边界、工具契约、数据模型、核心流程、外部依赖、权限规则等设计内容，必须先修改对应 Agent 文档，并将文档变更提交到本地 Git；在文档版本被锁定后，才能开始修改代码。  
> 禁止在同一次提交中混合 Agent 设计文档变更和对应代码实现变更，除非只是修正文档中的错别字、路径或示例。

---

## 1. Agent 定位与能力边界

- **背景**:  
  {{说明为什么需要该 Agent，它解决什么问题。}}

- **核心价值**:  
  {{用一句话说明该 Agent 的价值。}}

- **Agent 角色**:  
  {{用一句话描述该 Agent 的身份和职责。}}

- **核心目标**:
  1. {{目标 1}}
  2. {{目标 2}}
  3. {{目标 3}}

- **包含能力**:
  1. {{本 Agent 负责的能力 1}}
  2. {{本 Agent 负责的能力 2}}
  3. {{本 Agent 负责的能力 3}}

- **不包含能力**:
  1. {{本 Agent 不负责的事情 1}}
  2. {{本 Agent 不负责的事情 2}}
  3. {{本 Agent 不负责的事情 3}}

- **行为约束**:
  1. {{如：检索不到依据时必须说明，不得编造。}}
  2. {{如：删除、覆盖、合并等高风险操作必须确认。}}
  3. {{如：只能调用本文档声明的工具和服务。}}

---

## 2. Harness 架构与代码边界

> 本节说明 Agent Harness 的组成，以及各层职责边界。

- **Agent Loop 职责**:
  - {{如：接收用户输入。}}
  - {{如：构建 messages / system prompt / tools。}}
  - {{如：调用 LLM。}}
  - {{如：解析 tool_use。}}
  - {{如：执行工具并回填 tool_result。}}

- **Prompt Builder 职责**:
  - {{如：运行时拼接身份、规则、工具说明、上下文摘要。}}
  - {{如：不得承载具体业务数据的永久存储职责。}}

- **Tools 职责**:
  - {{如：作为 Agent 可执行动作的唯一入口。}}
  - {{如：封装查询、写入、检索、生成、外部调用等能力。}}

- **Services / Repositories 职责**:
  - {{如：负责真实业务逻辑、数据库读写、文件读写、外部 API 调用。}}
  - {{如：Tools 应通过 Service / Repository 完成底层操作。}}

- **Storage / External API 职责**:
  - {{如：保存业务事实、索引、日志、外部数据。}}

- **禁止绕过的边界**:
  1. Agent Loop 不得直接操作数据库或文件系统。
  2. LLM 输出不得被视为已持久化事实。
  3. 工具不得绕过权限规则执行高风险操作。
  4. 未在本文档声明的核心依赖不得擅自引入。

- **核心文件 / 目录**:

| 路径 | 职责 |
|---|---|
| `{{path_1}}` | {{职责说明}} |
| `{{path_2}}` | {{职责说明}} |
| `{{path_3}}` | {{职责说明}} |

---

## 3. 可调用工具与工具契约

### 3.1 工具列表

| 工具名 | 工具职责 | 调用时机 | 是否有副作用 | 是否需要确认 |
|---|---|---|---|---|
| `{{tool_name_1}}` | {{职责}} | {{调用时机}} | {{是/否}} | {{是/否}} |
| `{{tool_name_2}}` | {{职责}} | {{调用时机}} | {{是/否}} | {{是/否}} |
| `{{tool_name_3}}` | {{职责}} | {{调用时机}} | {{是/否}} | {{是/否}} |

### 3.2 工具契约

#### `{{tool_name_1}}`

- **职责**:  
  {{说明该工具负责什么。}}

- **输入**:

```json
{
  "{{field_1}}": "{{type / description}}",
  "{{field_2}}": "{{type / description}}"
}
```

- **输出**:

```json
{
  "{{field_1}}": "{{type / description}}",
  "{{field_2}}": "{{type / description}}"
}
```

- **副作用**:  
  {{说明是否写数据库、写文件、调用外部接口等。}}

- **失败处理**:  
  {{说明工具失败、输入非法、权限不足时应如何返回错误。}}

#### `{{tool_name_2}}`

- **职责**:  
  {{说明该工具负责什么。}}

- **输入**:

```json
{
  "{{field_1}}": "{{type / description}}"
}
```

- **输出**:

```json
{
  "{{field_1}}": "{{type / description}}"
}
```

- **副作用**:  
  {{说明是否写数据库、写文件、调用外部接口等。}}

- **失败处理**:  
  {{说明工具失败、输入非法、权限不足时应如何返回错误。}}

---

## 4. 上下文来源与记忆边界

- **运行时上下文来源**:
  1. {{如：用户当前输入。}}
  2. {{如：本地数据库检索结果。}}
  3. {{如：显式传入的配置或会话状态。}}

- **长期记忆来源**:
  1. {{如：SQLite 表。}}
  2. {{如：本地索引。}}

- **不得作为长期记忆的内容**:
  1. {{如：LLM 临时输出。}}
  2. {{如：未落库的对话上下文。}}

- **上下文裁剪规则**:
  1. {{说明上下文过长时如何取舍。}}
  2. {{说明哪些信息必须保留。}}

---

## 5. 核心业务流

### 5.1 {{业务流名称 1}}

1. {{步骤 1}}
2. {{步骤 2}}
3. {{步骤 3}}
4. {{步骤 4}}

- **成功条件**:  
  {{说明该流程成功完成的条件。}}

- **失败条件**:  
  {{说明该流程何时失败。}}

- **用户可见反馈**:  
  {{说明成功、失败、部分成功时如何反馈用户。}}

### 5.2 {{业务流名称 2}}

1. {{步骤 1}}
2. {{步骤 2}}
3. {{步骤 3}}

- **成功条件**:  
  {{说明该流程成功完成的条件。}}

- **失败条件**:  
  {{说明该流程何时失败。}}

- **用户可见反馈**:  
  {{说明成功、失败、部分成功时如何反馈用户。}}

---

## 6. 数据模型

### 6.1 {{model_name_1}}

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---|---|
| `{{field_1}}` | `{{type}}` | {{是/否}} | {{说明}} |
| `{{field_2}}` | `{{type}}` | {{是/否}} | {{说明}} |
| `{{field_3}}` | `{{type}}` | {{是/否}} | {{说明}} |

### 6.2 数据约束

1. {{如：id 必须稳定且唯一。}}
2. {{如：写入时间必须由系统生成。}}
3. {{如：来源字段不得为空。}}

---

## 7. 失败模式与降级策略

| 失败模式 | 触发条件 | Agent 行为 | 用户反馈 |
|---|---|---|---|
| {{失败模式 1}} | {{触发条件}} | {{行为}} | {{反馈}} |
| {{失败模式 2}} | {{触发条件}} | {{行为}} | {{反馈}} |
| {{失败模式 3}} | {{触发条件}} | {{行为}} | {{反馈}} |

- **通用降级原则**:
  1. {{如：工具失败时不得假装已完成。}}
  2. {{如：依据不足时必须明确说明。}}
  3. {{如：高风险操作失败时必须保留原始数据。}}

---

## 8. 测试要求

- **单元测试**:
  1. {{需要覆盖的 Service / Repository 行为。}}
  2. {{需要覆盖的工具输入输出契约。}}

- **集成测试**:
  1. {{需要覆盖的 Agent Loop + Tool 调用链路。}}
  2. {{需要覆盖的数据库或外部接口交互。}}

- **回归测试**:
  1. {{需要固定的关键用例。}}
  2. {{需要验证的边界场景。}}

- **验收清单**:
  1. {{如：符合本文档定义的能力边界。}}
  2. {{如：所有有副作用工具均可追踪。}}
  3. {{如：失败场景不会编造结果。}}

---

## 9. 变更记录

| 日期 | 变更内容 | 变更原因 | 提交 |
|---|---|---|---|
| `{{YYYY-MM-DD}}` | {{变更内容}} | {{变更原因}} | `{{commit_hash}}` |
